<h2>Edit line  item</h2>

<%= render "form.html", changeset: @changeset,
                        action: line__item_path(@conn, :update, @line__item) %>

<%= link "Back", to: line__item_path(@conn, :index) %>
